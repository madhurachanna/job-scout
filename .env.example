# LM Studio Configuration
LLM_BASE_URL=http://YOUR_WINDOWS_IP:1234/v1
LLM_MODEL_NAME=qwen3-8b
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2048

# Scraping Configuration
REQUEST_TIMEOUT=30
MAX_RETRIES=3

# Email Notifications (for --schedule mode)
# Gmail: use App Password from https://myaccount.google.com/apppasswords
# AWS SES: use SMTP credentials from SES console
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
NOTIFY_EMAIL=your-email@gmail.com

# Database (for tracking seen jobs)
# DB_PATH=data/job_scout.db
